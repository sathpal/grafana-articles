# LinkedIn post

Paste the block below as the post text. Attach `img/cover.png` as the image. Replace `[article link]` with the dev.to URL once published.

---

It is 02:10, an alert says "consumer lag high", and Grafana has forty dashboards.

I gave Grafana Cloud an open-source AI assistant to go through them for me. Two Apache-2.0 projects: Grafana's own MCP server (mcp-grafana) and Block's goose. It reads the same metrics, logs and traces my team reads, cites the query behind every claim, and I can swap the model, audit every call and put it on a budget.

Then I pointed it at a deliberately messy pipeline: Python API, Kafka, a Java consumer, a Go image renderer, a Redis cache tier, a batch job. Four languages, one trace across all of it.

Part 1 of 6 is out: why, and what the assistant will be debugging. Setup, three real incidents and the guard rails follow over the next two weeks.

Full article: [article link]

#grafana #observability #opentelemetry #ai #opensource #sre
