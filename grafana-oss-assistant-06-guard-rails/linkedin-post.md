# LinkedIn post

Paste the block below as the post text. Attach `img/cover.png` as the image. Replace `[article link]` with the dev.to URL once published.

---

The assistant solved three incidents. It also reported a p95 of 4.75 seconds that did not exist.

Part 6 closes the series with what went wrong and what contained it: broken histogram buckets it faithfully repeated, metric names it would have invented without a rule, 81 tools when it needed 15. And the controls: a read-only server by default, scoped write tokens, fewer tools, one server per team with an audit log, pinned models, recipes in git, a budget.

The five things that actually made it work: change annotations, one service_name across metrics, logs and traces, trace context through Kafka, six lines of house rules, and the exporters you would run anyway.

Everything is reproducible from the repo. Thanks for reading all six.

Full article: [article link]

#grafana #observability #opentelemetry #ai #opensource #sre
