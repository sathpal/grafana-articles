# LinkedIn post

Paste the block below as the post text. Attach `img/cover.png` as the image. Replace `[article link]` with the dev.to URL once published.

---

Fifteen minutes from a Grafana Cloud stack to an open-source AI assistant that can query it.

Part 2 of my series is the how: one service-account token, the Grafana MCP server on a port of your choice, goose attached as an extension, six lines of "house rules" that stop the model from inventing metric names, and a smoke test that proves the wiring before you trust anything it says.

The rule that matters most: never assert a cause without a query result that shows it. Everything else follows from that.

Recipes in the repo make each investigation one command.

Full article: [article link]

#grafana #observability #mcp #ai #opensource #sre
